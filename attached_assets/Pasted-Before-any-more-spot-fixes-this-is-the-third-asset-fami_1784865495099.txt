Before any more spot fixes: this is the third asset family broken by the artifact.toml rerouting (logos, materials images, now frame-finish swatches are blank on abaco-club-chair-shoreline). Each was found by human clicking. That ends now. You will do a systematic blast-radius audit of the serving change and verify every affected surface, not just the one I found.

Step 1 — Define the blast radius. List every path prefix that artifact.toml now routes to the API server instead of the static web artifact. This set of prefixes is the complete universe of what your change can have broken.

Step 2 — Enumerate everything living under those prefixes, from three sources: (a) every file and directory in web/public whose URL would fall under a rerouted prefix; (b) every URL pattern the frontend code requests (grep the client code and the API responses for URL-building: image paths, storage paths, asset paths, route links); (c) every image/file path stored in the database (logo_url, image paths on products/finishes/fabrics/materials/categories, any column holding a URL or path). Produce one table: URL family, example URL, where it's served now, expected content type.

Step 3 — Verify every row with a real request: correct status AND correct content-type (image bytes for images, JSON for API, HTML shell only for documents). Paste the table with results.

Step 4 — Separately, run a functional pass on every distinct page type and flow the app has, as a user would reach them: home, shop grid + each filter type, category page, product detail (one purchasable, one quote-only, one Shoreline-type with swatches), finish/fabric pickers opening with images, search, manufacturers list + detail, materials page, commercial, contact, cart add/view, checkout up to payment, wishlist, staff login page loads, admin loads behind auth. For each: does the page render, do its images load, do its data calls return 200 JSON. Report each with pass/fail and evidence (network requests), not a summary sentence.

Step 5 — Explain why your previous express.static fix did not cover the swatch images. I want the gap analysis: what assumption was wrong. Then fix all failures found, and state whether the 3 console errors from earlier are these same failing requests.

Nothing is "verified" by reasoning or by shell-status-200 alone. Every image check must confirm content-type. Dev only. Paste actual output for every step.