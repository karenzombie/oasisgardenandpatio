Step 2 approved. Items 4, 6 and 7 are correct, and item 5 is confirmed
left alone. I verified no imports or variables were orphaned by the
deletions: Button is still used by the empty-cart block, Spinner by the
loading states, and isAuthenticated still has multiple remaining uses.

Proceed to Step 3: items 8, 9, 10 and 11. This is the step that actually
makes guest checkout work, so it must be complete before anything is
deployed.

  - Item 8: in handleHostedFormSubmit, the place-order payload is built
    from the address payload and the payment token only. The guest form
    state collected by the Contact section is never sent. For a guest,
    include guestContact with email, firstName, lastName and phone,
    trimmed. For a signed-in user, omit the field entirely. The field
    already exists in the OpenAPI spec and in the generated client
    types, so there is no spec change and no codegen run.

  - Item 9: the completeness check that gates the HostedForm button
    currently validates address fields only. Extend it so that, for
    guests only, the button also stays disabled until email matches the
    existing EMAIL_RE constant, firstName and lastName are non-empty
    after trim, and phone is at least 7 characters after trim. Those
    minimums mirror the GuestContact schema in the spec. EMAIL_RE is
    already declared in this file and is currently unused. Use it. Do
    not add a second regex.

  - Item 10: do not change how the payload falls through to inline
    address objects. Guests must never send a saved address id. The
    backend rejects that with a 400.

  - Item 11: add a short inline message near the Place Order button, in
    the same style as the existing "Fill in your billing and shipping
    address above to enable payment." hint, telling a guest to complete
    their contact info. No toast.

Do not touch anything in the "Do NOT touch" list, in particular the
orderSubmittedRef double-submit latch and its reset rules, and the
criticalError behavior for 500 and 503.

STOP after Step 3. Report the diff, paste the typecheck output, and wait
for confirmation. Do not run any UI verification yourself.